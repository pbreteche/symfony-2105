{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_widget(form) }}
    <button>Modifier</button>
    {{ form_end(form) }}
{% endblock %}
